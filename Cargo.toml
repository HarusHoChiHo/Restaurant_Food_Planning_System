[package]
name = "RFPS-Rust"
version = "0.1.0"
edition = "2024"

[workspace]
members = [".", "entity", "handler", "migration"]

[dependencies]
axum = "0.8.4"
tokio = { version = "1.44.2", features = ["full"]}
handler = { path = "handler"}
